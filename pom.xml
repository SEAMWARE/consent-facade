<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.micronaut.platform</groupId>
        <artifactId>micronaut-parent</artifactId>
        <version>4.7.1</version>
    </parent>

    <groupId>org.fiware.consent</groupId>
    <artifactId>consent-facade</artifactId>
    <version>0.0.1</version>

    <description>${project.description}</description>
    <url>${project.url}</url>
    <licenses>
        <license>
            <name>${project.license.name}</name>
            <url>${project.license.url}</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>${project.author.name}</name>
            <email>${project.author.email}</email>
        </developer>
    </developers>

    <properties>
        <jdk.version>21</jdk.version>
        <release.version>21</release.version>
        <micronaut.version>4.7.1</micronaut.version>

        <!-- project info -->
        <project.author.name>Stefan Wiedemann</project.author.name>
        <project.author.email>stefan.wiedemann@fiware.org</project.author.email>
        <project.description>Facade exposing the Prometheus-X contract-service API on top of the TM Forum APIs, for the
            consent-manager.
        </project.description>
        <project.summary>Projects TM Forum product orders and catalogs into the contracts and catalog self-descriptions
            the consent-manager consumes.
        </project.summary>
        <project.url>https://github.com/wistefan/consent-facade</project.url>
        <project.license.name>MIT</project.license.name>
        <project.license.identifier>MIT</project.license.identifier>
        <project.license.url>https://opensource.org/license/mit/</project.license.url>
        <project.title>Consent Facade</project.title>
        <project.vendor>FIWARE Foundation, e.V.</project.vendor>

        <!-- own API, provided towards the consent-manager -->
        <consent-facade.api>${project.basedir}/api/consent-facade.yaml</consent-facade.api>
        <consent-facade-internal.api>${project.basedir}/api/consent-facade-internal.yaml</consent-facade-internal.api>

        <!-- TM Forum APIs (consumed) -->
        <tmforum.productorder.api.url>
            https://raw.githubusercontent.com/FIWARE/tmforum-api/refs/heads/main/api/tm-forum/product-ordering-management/api.json
        </tmforum.productorder.api.url>
        <tmforum.productcatalog.api.url>
            https://raw.githubusercontent.com/FIWARE/tmforum-api/refs/heads/main/api/tm-forum/product-catalog/api.json
        </tmforum.productcatalog.api.url>
        <tmforum.productinventory.api.url>
            https://raw.githubusercontent.com/FIWARE/tmforum-api/refs/heads/main/api/tm-forum/product-inventory/api.json
        </tmforum.productinventory.api.url>
        <tmforum.party.api.url>
            https://raw.githubusercontent.com/FIWARE/tmforum-api/refs/heads/main/api/tm-forum/party-catalog/api.json
        </tmforum.party.api.url>
        <tmforum.agreement.api.url>
            https://raw.githubusercontent.com/FIWARE/tmforum-api/refs/heads/main/api/tm-forum/agreement/api.json
        </tmforum.agreement.api.url>

        <!-- oci -->
        <exec.mainClass>org.fiware.consent.Application</exec.mainClass>
        <image.registry>quay.io</image.registry>
        <image.repository>wi_stefan</image.repository>
        <image.tag>${project.version}</image.tag>

        <!-- lazy dev -->
        <version.org.projectlombok>1.18.36</version.org.projectlombok>

        <!-- logging -->

        <!-- code gen -->
        <version.org.openapitools.generator-maven-plugin>7.10.0</version.org.openapitools.generator-maven-plugin>
        <version.io.kokuwa.micronaut.codegen>4.4.1</version.io.kokuwa.micronaut.codegen>

        <!-- test -->
        <version.org.awaitility>4.2.0</version.org.awaitility>
        <version.io.github.wistefan.oid4vp-client-lib>0.0.5</version.io.github.wistefan.oid4vp-client-lib>
        <version.com.nimbusds.nimbus-jose-jwt>10.5</version.com.nimbusds.nimbus-jose-jwt>
        <version.org.bouncycastle>1.81</version.org.bouncycastle>

        <!-- build -->
        <version.org.codehaus.mojo.build-helper-maven-plugin>3.6.0</version.org.codehaus.mojo.build-helper-maven-plugin>
        <version.org.jacoco.maven-plugin>0.8.12</version.org.jacoco.maven-plugin>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${version.org.projectlombok}</version>
            <scope>provided</scope>
        </dependency>
        <!-- micronaut -->
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-runtime</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-management</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-inject</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-http-server-netty</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-http-client</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut.validation</groupId>
            <artifactId>micronaut-validation</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-jackson-databind</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut.reactor</groupId>
            <artifactId>micronaut-reactor</artifactId>
            <scope>compile</scope>
        </dependency>

        <!-- persistence (only active when the persistent provider registry is enabled,
             facade.provider-registry.persistent=true; see PersistentProviderRegistry).
             The registry runs on PostgreSQL; the schema is managed by Flyway. -->
        <dependency>
            <groupId>io.micronaut.data</groupId>
            <artifactId>micronaut-data-jdbc</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut.sql</groupId>
            <artifactId>micronaut-jdbc-hikari</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut.flyway</groupId>
            <artifactId>micronaut-flyway</artifactId>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- tests exercise the same Flyway migration against H2 in PostgreSQL-compatibility mode
             (H2 support ships in flyway-core; only PostgreSQL needs its own module, above) -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- OID4VP authentication of outbound TM Forum calls (active only when oid4vp.enabled=true) -->
        <dependency>
            <groupId>io.github.wistefan</groupId>
            <artifactId>oid4vp-client-lib</artifactId>
            <version>${version.io.github.wistefan.oid4vp-client-lib}</version>
        </dependency>
        <dependency>
            <groupId>com.nimbusds</groupId>
            <artifactId>nimbus-jose-jwt</artifactId>
            <version>${version.com.nimbusds.nimbus-jose-jwt}</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>${version.org.bouncycastle}</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>${version.org.bouncycastle}</version>
        </dependency>

        <!-- logging -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.micronaut.test</groupId>
            <artifactId>micronaut-test-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>${version.org.awaitility}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- generate server API (own) + client APIs (TM Forum) -->
            <plugin>
                <groupId>org.openapitools</groupId>
                <artifactId>openapi-generator-maven-plugin</artifactId>
                <version>${version.org.openapitools.generator-maven-plugin}</version>
                <executions>
                    <!-- our API, provided towards the consent-manager (server interfaces) -->
                    <execution>
                        <id>consent-facade</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${consent-facade.api}</inputSpec>
                            <apiPackage>org.fiware.consent.api</apiPackage>
                            <modelPackage>org.fiware.consent.model</modelPackage>
                            <strictSpec>true</strictSpec>
                            <generateAliasAsModel>true</generateAliasAsModel>
                            <generateApiTests>false</generateApiTests>
                            <generatorName>micronaut</generatorName>
                            <modelNameSuffix>VO</modelNameSuffix>
                            <output>${project.build.directory}</output>
                            <configOptions>
                                <fullJavaUtil>true</fullJavaUtil>
                                <useOptional>false</useOptional>
                                <useGenericResponse>true</useGenericResponse>
                                <useLombokGenerated>true</useLombokGenerated>
                                <introspected>true</introspected>
                                <useBeanValidation>false</useBeanValidation>
                                <supportAsync>true</supportAsync>
                                <jacksonDatabindNullable>false</jacksonDatabindNullable>
                                <serdeable>false</serdeable>
                                <generateExamples>false</generateExamples>
                            </configOptions>
                            <typeMappings>
                                <typeMapping>java.util.Date=java.time.Instant</typeMapping>
                            </typeMappings>
                        </configuration>
                    </execution>
                    <!-- our INTERNAL API (server interfaces). Separate spec + package: it is not part of
                         the consent-manager-facing contract and must never be published. supportAsync
                         is off because both endpoints block (JDBC registry, OID4VP exchange). -->
                    <execution>
                        <id>consent-facade-internal</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${consent-facade-internal.api}</inputSpec>
                            <apiPackage>org.fiware.consent.internal.api</apiPackage>
                            <modelPackage>org.fiware.consent.internal.model</modelPackage>
                            <strictSpec>true</strictSpec>
                            <generateAliasAsModel>true</generateAliasAsModel>
                            <generateApiTests>false</generateApiTests>
                            <generatorName>micronaut</generatorName>
                            <modelNameSuffix>VO</modelNameSuffix>
                            <output>${project.build.directory}</output>
                            <generateSupportingFiles>false</generateSupportingFiles>
                            <configOptions>
                                <fullJavaUtil>true</fullJavaUtil>
                                <useOptional>false</useOptional>
                                <useGenericResponse>true</useGenericResponse>
                                <useLombokGenerated>true</useLombokGenerated>
                                <introspected>true</introspected>
                                <useBeanValidation>false</useBeanValidation>
                                <supportAsync>false</supportAsync>
                                <jacksonDatabindNullable>false</jacksonDatabindNullable>
                                <serdeable>false</serdeable>
                                <generateExamples>false</generateExamples>
                            </configOptions>
                            <typeMappings>
                                <typeMapping>java.util.Date=java.time.Instant</typeMapping>
                            </typeMappings>
                        </configuration>
                    </execution>
                    <!-- TM Forum product ordering (client) -->
                    <execution>
                        <id>tmforum-product-ordering</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${tmforum.productorder.api.url}</inputSpec>
                            <apiPackage>org.fiware.consent.tmforum.productorder.api</apiPackage>
                            <modelPackage>org.fiware.consent.tmforum.productorder.model</modelPackage>
                            <strictSpec>true</strictSpec>
                            <generatorName>micronaut</generatorName>
                            <modelNameSuffix>VO</modelNameSuffix>
                            <output>${project.build.directory}</output>
                            <generateSupportingFiles>false</generateSupportingFiles>
                            <configOptions>
                                <clientId>product-order</clientId>
                                <fullJavaUtil>true</fullJavaUtil>
                                <useOptional>false</useOptional>
                                <useGenericResponse>true</useGenericResponse>
                                <useLombokGenerated>true</useLombokGenerated>
                                <introspected>true</introspected>
                                <useBeanValidation>false</useBeanValidation>
                                <supportAsync>true</supportAsync>
                                <jacksonDatabindNullable>false</jacksonDatabindNullable>
                                <serdeable>false</serdeable>
                                <generateExamples>false</generateExamples>
                            </configOptions>
                            <typeMappings>
                                <typeMapping>java.util.Date=java.time.Instant</typeMapping>
                            </typeMappings>
                        </configuration>
                    </execution>
                    <!-- TM Forum product catalog (client) -->
                    <execution>
                        <id>tmforum-product-catalog</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${tmforum.productcatalog.api.url}</inputSpec>
                            <apiPackage>org.fiware.consent.tmforum.productcatalog.api</apiPackage>
                            <modelPackage>org.fiware.consent.tmforum.productcatalog.model</modelPackage>
                            <strictSpec>true</strictSpec>
                            <generatorName>micronaut</generatorName>
                            <modelNameSuffix>VO</modelNameSuffix>
                            <output>${project.build.directory}</output>
                            <generateSupportingFiles>false</generateSupportingFiles>
                            <configOptions>
                                <clientId>product-catalog</clientId>
                                <fullJavaUtil>true</fullJavaUtil>
                                <useOptional>false</useOptional>
                                <useGenericResponse>true</useGenericResponse>
                                <useLombokGenerated>true</useLombokGenerated>
                                <introspected>true</introspected>
                                <useBeanValidation>false</useBeanValidation>
                                <supportAsync>true</supportAsync>
                                <jacksonDatabindNullable>false</jacksonDatabindNullable>
                                <serdeable>false</serdeable>
                                <generateExamples>false</generateExamples>
                            </configOptions>
                            <typeMappings>
                                <typeMapping>java.util.Date=java.time.Instant</typeMapping>
                            </typeMappings>
                        </configuration>
                    </execution>
                    <!-- TM Forum party catalog (client) -->
                    <execution>
                        <id>tmforum-party</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${tmforum.party.api.url}</inputSpec>
                            <apiPackage>org.fiware.consent.tmforum.party.api</apiPackage>
                            <modelPackage>org.fiware.consent.tmforum.party.model</modelPackage>
                            <strictSpec>true</strictSpec>
                            <generatorName>micronaut</generatorName>
                            <modelNameSuffix>VO</modelNameSuffix>
                            <output>${project.build.directory}</output>
                            <generateSupportingFiles>false</generateSupportingFiles>
                            <configOptions>
                                <clientId>party</clientId>
                                <fullJavaUtil>true</fullJavaUtil>
                                <useOptional>false</useOptional>
                                <useGenericResponse>true</useGenericResponse>
                                <useLombokGenerated>true</useLombokGenerated>
                                <introspected>true</introspected>
                                <useBeanValidation>false</useBeanValidation>
                                <supportAsync>true</supportAsync>
                                <jacksonDatabindNullable>false</jacksonDatabindNullable>
                                <serdeable>false</serdeable>
                                <generateExamples>false</generateExamples>
                            </configOptions>
                            <typeMappings>
                                <typeMapping>java.util.Date=java.time.Instant</typeMapping>
                            </typeMappings>
                        </configuration>
                    </execution>
                    <!-- TM Forum product inventory (client) -->
                    <execution>
                        <id>tmforum-product-inventory</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${tmforum.productinventory.api.url}</inputSpec>
                            <apiPackage>org.fiware.consent.tmforum.productinventory.api</apiPackage>
                            <modelPackage>org.fiware.consent.tmforum.productinventory.model</modelPackage>
                            <strictSpec>true</strictSpec>
                            <generatorName>micronaut</generatorName>
                            <modelNameSuffix>VO</modelNameSuffix>
                            <output>${project.build.directory}</output>
                            <generateSupportingFiles>false</generateSupportingFiles>
                            <configOptions>
                                <clientId>product-inventory</clientId>
                                <fullJavaUtil>true</fullJavaUtil>
                                <useOptional>false</useOptional>
                                <useGenericResponse>true</useGenericResponse>
                                <useLombokGenerated>true</useLombokGenerated>
                                <introspected>true</introspected>
                                <useBeanValidation>false</useBeanValidation>
                                <supportAsync>true</supportAsync>
                                <jacksonDatabindNullable>false</jacksonDatabindNullable>
                                <serdeable>false</serdeable>
                                <generateExamples>false</generateExamples>
                            </configOptions>
                            <typeMappings>
                                <typeMapping>java.util.Date=java.time.Instant</typeMapping>
                            </typeMappings>
                        </configuration>
                    </execution>
                    <!-- TM Forum agreement (client) -->
                    <execution>
                        <id>tmforum-agreement</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate</goal>
                        </goals>
                        <configuration>
                            <inputSpec>${tmforum.agreement.api.url}</inputSpec>
                            <apiPackage>org.fiware.consent.tmforum.agreement.api</apiPackage>
                            <modelPackage>org.fiware.consent.tmforum.agreement.model</modelPackage>
                            <strictSpec>true</strictSpec>
                            <generatorName>micronaut</generatorName>
                            <modelNameSuffix>VO</modelNameSuffix>
                            <output>${project.build.directory}</output>
                            <generateSupportingFiles>false</generateSupportingFiles>
                            <configOptions>
                                <clientId>agreement</clientId>
                                <fullJavaUtil>true</fullJavaUtil>
                                <useOptional>false</useOptional>
                                <useGenericResponse>true</useGenericResponse>
                                <useLombokGenerated>true</useLombokGenerated>
                                <introspected>true</introspected>
                                <useBeanValidation>false</useBeanValidation>
                                <supportAsync>true</supportAsync>
                                <jacksonDatabindNullable>false</jacksonDatabindNullable>
                                <serdeable>false</serdeable>
                                <generateExamples>false</generateExamples>
                            </configOptions>
                            <typeMappings>
                                <typeMapping>java.util.Date=java.time.Instant</typeMapping>
                            </typeMappings>
                        </configuration>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>io.kokuwa.micronaut</groupId>
                        <artifactId>micronaut-openapi-codegen</artifactId>
                        <version>${version.io.kokuwa.micronaut.codegen}</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!-- add the generated sources to the build -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>${version.org.codehaus.mojo.build-helper-maven-plugin}</version>
                <executions>
                    <execution>
                        <id>openapi-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>${project.build.directory}/generated-sources/openapi</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--
                Register Lombok as an annotation processor. The micronaut-parent pins
                annotationProcessorPaths to the Micronaut processor only, which drops Lombok
                (a provided-scope dependency) from the processor path and turns its annotations
                into no-ops. Lombok must precede micronaut-inject-java so the members it generates
                (constructors, getters/setters, loggers) are visible when Micronaut builds its bean
                metadata.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths combine.self="override">
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${version.org.projectlombok}</version>
                        </path>
                        <path>
                            <groupId>io.micronaut</groupId>
                            <artifactId>micronaut-inject-java</artifactId>
                            <version>${micronaut.core.version}</version>
                        </path>
                        <!-- compiles @MappedEntity/@Repository metadata for the persistent
                             provider registry (Phase 6) -->
                        <path>
                            <groupId>io.micronaut.data</groupId>
                            <artifactId>micronaut-data-processor</artifactId>
                            <version>${micronaut.data.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>io.micronaut.maven</groupId>
                <artifactId>micronaut-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>@{argLine} --add-opens java.base/java.lang=ALL-UNNAMED --add-opens
                        java.base/java.time=ALL-UNNAMED
                    </argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${version.org.jacoco.maven-plugin}</version>
                <configuration>
                    <!-- exclude generated code -->
                    <excludes>
                        <exclude>org/fiware/consent/api/**/*</exclude>
                        <exclude>org/fiware/consent/model/**/*</exclude>
                        <exclude>org/fiware/consent/tmforum/**/api/**/*</exclude>
                        <exclude>org/fiware/consent/tmforum/**/model/**/*</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- build and (optionally) push the OCI image via jib -->
        <profile>
            <id>oci</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.google.cloud.tools</groupId>
                        <artifactId>jib-maven-plugin</artifactId>
                        <configuration combine.self="override">
                            <from>
                                <image>registry.access.redhat.com/ubi8/openjdk-21:1.21-1</image>
                                <platforms>
                                    <platform>
                                        <architecture>amd64</architecture>
                                        <os>linux</os>
                                    </platform>
                                    <platform>
                                        <architecture>arm64</architecture>
                                        <os>linux</os>
                                    </platform>
                                </platforms>
                            </from>
                            <to>
                                <image>${image.registry}/${image.repository}/${project.artifactId}</image>
                                <tags>
                                    <tag>${image.tag}</tag>
                                </tags>
                            </to>
                            <container>
                                <labels>
                                    <org.opencontainers.image.description>${project.description}
                                    </org.opencontainers.image.description>
                                    <org.opencontainers.image.documentation>${project.url}
                                    </org.opencontainers.image.documentation>
                                    <org.opencontainers.image.licenses>${project.license.identifier}
                                    </org.opencontainers.image.licenses>
                                    <org.opencontainers.image.source>${project.url}</org.opencontainers.image.source>
                                    <org.opencontainers.image.title>${project.title}</org.opencontainers.image.title>
                                    <org.opencontainers.image.vendor>${project.vendor}</org.opencontainers.image.vendor>
                                </labels>
                            </container>
                        </configuration>
                        <!-- push the image to the registry during the deploy phase (CI: mvn clean deploy -Poci) -->
                        <executions>
                            <execution>
                                <id>push-oci-image</id>
                                <phase>deploy</phase>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!--
                        The deploy phase publishes the OCI image via jib, not a Maven artifact to a
                        repository. Disable the default maven-deploy-plugin, which would otherwise
                        fail for want of a distributionManagement repository.
                    -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-deploy-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
